getopt-win32 provides:

  The getopt library provides POSIX-style getopt() function on Windows.

Usage (CMake):

  target_link_libraries(main PRIVATE getopt)
  target_include_directories(main PRIVATE "${CMAKE_CURRENT_LIST_DIR}/vcpkg_installed/include")
